struct MaxEntropyOptimizer
    n_elite::Int # Number of best performing parameters to push ahead
    n_test::Int # Total number of parameters to test
    test
end

function evaluate(opt::MaxEntropyOptimizer)

end
